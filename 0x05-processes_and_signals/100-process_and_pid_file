#!/usr/bin/env bash

killed() {
	rm /var/run/bamansourscript.pid
	exit
}

echo "$$" > /var/run/bamansourscript.pid

while true
do
	echo "To infinity and beyond"
	sleep 2
	trap 'echo "Y U no love me?!"' SIGINT
	trap 'echo "I hate the kill command" && killed' SIGTERM
	trap 'killed' SIGQUIT
done
